'use strict';

/**
 * lokasi-kelantan controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::lokasi-kelantan.lokasi-kelantan');
