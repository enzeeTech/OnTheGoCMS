'use strict';

/**
 * lokasi-puala-pinang router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::lokasi-puala-pinang.lokasi-puala-pinang');
